import 'package:flutter/material.dart';

class AdvertisementBanners extends StatelessWidget {
  const AdvertisementBanners({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        border: Border.all(),
      ),
      child: Placeholder(),
    );
  }
}
